version: 8.11.3

service:
  type: LoadBalancer  # LoadBalancer
  port: 9200

master:
  replicas: 3
  storage:
    size: 5Gi
    storageClassName: local-path

data:
  replicas: 3
  storage:
    size: 10Gi
    storageClassName: local-path

