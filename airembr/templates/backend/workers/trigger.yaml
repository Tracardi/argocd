{{ if and .Values.secrets.dockerHub .Values.worker.trigger.enabled .Values.pulsar.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wk-trigger
  labels:
    app: wk-trigger
    version: {{ .Chart.AppVersion | quote }}
    {{- include "airembr.labels" (dict "ctx" . "component" "wk-trigger") | nindent 4 }}
spec:
  replicas: {{ .Values.worker.trigger.replicas }}
  selector:
    matchLabels:
      {{- include "airembr.selectorLabels" (dict "ctx" . "component" "wk-trigger") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "airembr.podLabels" (dict "ctx" . "component" "wk-trigger") | nindent 8 }}
    spec:
      {{- include "chart.nodeAffinity" (dict "context" . "path" "worker.trigger") | nindent 6 }}

      {{ if .Values.worker.trigger.spread }}
        {{range .Values.worker.trigger.spread }}
          {{ toYaml . | nindent 6 }}
        {{ end }}
      {{ end }}

      imagePullSecrets:
        - name: {{ .Values.secrets.dockerHub | quote }}
      initContainers:
        - name: pulsar-init
          image: {{ .Values.worker.trigger.image.init }}:{{ .Values.worker.trigger.image.tag }}
          imagePullPolicy: IfNotPresent
          env:
            {{- include "airembr.env" (dict "ctx" .) | nindent 12 }}
            - name: LOGGING_LEVEL
              value: debug
      containers:
        - name: wk-trigger
          image: "{{ .Values.worker.trigger.image.repository }}:{{ .Values.worker.trigger.image.tag }}"
          imagePullPolicy: {{ .Values.worker.trigger.image.pullPolicy }}
          resources:
            limits:
              memory: {{ .Values.worker.trigger.resources.limits.memory }}
              cpu: {{ .Values.worker.trigger.resources.limits.cpu }}
            requests:
              memory: {{ .Values.worker.trigger.resources.requests.memory }}
              cpu: {{ .Values.worker.trigger.resources.requests.cpu }}
          env:
            {{- include "airembr.env" (dict "ctx" .) | nindent 12 }}
            - name: LOGGING_LEVEL
              value: {{ .Values.worker.trigger.config.loggingLevel | quote }}
            - name: INACTIVITY_TIME_OUT
              value: {{ .Values.worker.trigger.config.bulker.bufferInactivityTimeOut | quote }}
            - name: CONSUMER_TYPE
              value: "system.destination"

          volumeMounts:
            - name: pre-resources-vol
              mountPath: "/system/system/db/preconfig/data/resources.json"
              subPath: "resources.json"
            - name: pre-destinations-vol
              mountPath: "/system/system/db/preconfig/data/destinations.json"
              subPath: "destinations.json"
            - name: pre-event-sources-vol
              mountPath: "/system/system/db/preconfig/data/event-sources.json"
              subPath: "event-sources.json"
            - name: tenant-aliases-vol
              mountPath: "/system/system/db/preconfig/data/tenant-aliases.json"
              subPath: "tenant-aliases.json"

      volumes:
        - name: pre-resources-vol
          configMap:
            name: pre-resources-cm

        - name: pre-destinations-vol
          configMap:
            name: pre-destinations-cm

        - name: pre-event-sources-vol
          configMap:
            name: pre-event-sources-cm

        - name: tenant-aliases-vol
          configMap:
            name: tenant-aliases-cm
{{ end }}