{{ if and .Values.secrets.dockerHub .Values.job.imap.enabled .Values.pulsar.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: job-imap
  labels:
    app: job-imap
    version: {{ .Chart.AppVersion | quote }}
    {{- include "airembr.labels" (dict "ctx" . "component" "job-imap") | nindent 4 }}
spec:
  schedule: "*/30 * * * *" # every 30 minutes
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            {{- include "airembr.podLabels" (dict "ctx" . "component" "job-imap") | nindent 12 }}
        spec:
          {{- include "chart.nodeAffinity" (dict "context" . "path" "job.imap") | nindent 10 }}
          {{ if .Values.job.imap.spread }}
            {{range .Values.job.imap.spread }}
              {{ toYaml . | nindent 10 }}
            {{ end }}
          {{ end }}
          imagePullSecrets:
            - name: {{ .Values.secrets.dockerHub | quote }}
          restartPolicy: OnFailure
          containers:
            - name: job-imap
              image: "{{ .Values.job.imap.image.repository }}:{{ .Values.job.imap.image.tag }}"
              imagePullPolicy: {{ .Values.job.imap.image.pullPolicy }}
              resources:
                limits:
                  memory: {{ .Values.job.imap.resources.limits.memory }}
                  cpu: {{ .Values.job.imap.resources.limits.cpu }}
                requests:
                  memory: {{ .Values.job.imap.resources.requests.memory }}
                  cpu: {{ .Values.job.imap.resources.requests.cpu }}
              env:
                {{- include "airembr.env" (dict "ctx" .) | nindent 16 }}
                - name: LOGGING_LEVEL
                  value: {{ .Values.job.imap.config.loggingLevel | quote }}
{{ end }}
