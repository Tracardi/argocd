{{ if and .Values.secrets.dockerHub .Values.api.gui.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gui
  labels:
    app: api-gui
    version: {{ .Chart.AppVersion | quote }}
    {{- include "tracardi.labels" (dict "ctx" . "component" "api-gui") | nindent 4 }}
spec:
  replicas: {{ .Values.api.gui.replicas }}
  selector:
    matchLabels:
      {{- include "tracardi.selectorLabels" (dict "ctx" . "component" "api-gui") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tracardi.podLabels" (dict "ctx" . "component" "api-gui") | nindent 8 }}
    spec:
      {{- include "chart.nodeAffinity" (dict "context" . "path" "api.private") | nindent 6 }}
      imagePullSecrets:
        - name: {{ .Values.secrets.dockerHub  | quote }}
      initContainers:
        - name: tracardi-init
          image: tracardi/init:{{ .Values.api.gui.image.tag }}
          imagePullPolicy: Always
          env:
            {{- include "tracardi.env" (dict "ctx" .) | nindent 12 }}
            - name: LOGGING_LEVEL
              value: debug
      containers:
        - name: api-gui
          {{- include "tracardi.image" (dict "ctx" . "repo" .Values.api.gui.image.repository "tag" .Values.api.gui.image.tag "policy" .Values.api.gui.image.pullPolicy) | nindent 10 }}
          resources:
            limits:
              memory: {{ .Values.api.gui.resources.limits.memory }}
              cpu: {{ .Values.api.gui.resources.limits.cpu }}
            requests:
              memory: {{ .Values.api.gui.resources.requests.memory }}
              cpu: {{ .Values.api.gui.resources.requests.cpu }}
          ports:
            - name: private-port
              containerPort: 80
          {{- include "tracardi.livenessProbe" . | nindent 10 }}
          env:
            {{- include "tracardi.env" (dict "ctx" .) | nindent 12 }}
            - name: DEMO
              value: {{ .Values.config.demo | quote }}
            - name: EXPOSE_GUI_API
              value: {{ .Values.api.gui.config.enableGuiApi | quote }}
            - name: PRODUCTION
              value: "no"
            - name: LOGGING_LEVEL
              value: {{ .Values.api.gui.config.loggingLevel | quote }}
            - name: API_DOCS
              value: {{ .Values.api.gui.config.apiDocs | quote}}
            - name: SAVE_LOGS
              value: {{ .Values.api.gui.config.saveLogs | quote }}
            - name: SERVER_LOGGING_LEVEL
              value: {{ .Values.api.gui.config.serverLoggingLevel | quote }}
            - name: ELASTIC_LOGGING_LEVEL
              value: {{ .Values.api.gui.config.elasticloggingLevel | quote }}

          volumeMounts:
            {{- include "tracardi.volumeMounts" (dict "ctx" .) | nindent 12 }}

      volumes:
        {{- include "tracardi.volumes" (dict "ctx" .) | nindent 8 }}

{{ end }}


