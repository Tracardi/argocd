{{ if and .Values.secrets.dockerHub .Values.worker.properties.enabled .Values.pulsar.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wk-bg-properties
  labels:
    app: wk-bg-properties
    version: {{ .Chart.AppVersion | quote }}
    {{- include "tracardi.labels" (dict "ctx" . "component" "wk-bg-properties") | nindent 4 }}
spec:
  replicas: {{ .Values.worker.properties.replicas }}
  selector:
    matchLabels:
      {{- include "tracardi.selectorLabels" (dict "ctx" . "component" "wk-bg-properties") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tracardi.podLabels" (dict "ctx" . "component" "wk-bg-properties") | nindent 8 }}
    spec:
      {{- include "chart.nodeAffinity" (dict "context" . "path" "worker.properties") | nindent 6 }}

      {{ if .Values.worker.properties.spread }}
        {{range .Values.worker.properties.spread }}
          {{ toYaml . | nindent 6 }}
        {{ end }}
      {{ end }}

      imagePullSecrets:
        - name: {{ .Values.secrets.dockerHub | quote }}
      initContainers:
        - name: tracardi-init
          image: tracardi/init:{{ .Values.api.collector.image.tag }}
          imagePullPolicy: Always
          env:
            {{- include "tracardi.env" (dict "ctx" .) | nindent 12 }}
            - name: LOGGING_LEVEL
              value: debug
      containers:
        - name: wk-bg-properties
          image: "{{ .Values.worker.properties.image.repository }}:{{ .Values.worker.properties.image.tag }}"
          imagePullPolicy: {{ .Values.worker.properties.image.pullPolicy }}
          resources:
            limits:
              memory: {{ .Values.worker.properties.resources.limits.memory }}
              cpu: {{ .Values.worker.properties.resources.limits.cpu }}
            requests:
              memory: {{ .Values.worker.properties.resources.requests.memory }}
              cpu: {{ .Values.worker.properties.resources.requests.cpu }}
          env:
            {{- include "tracardi.env" (dict "ctx" .) | nindent 12 }}
            - name: LOGGING_LEVEL
              value: {{ .Values.worker.properties.config.loggingLevel | quote }}
            - name: INACTIVITY_TIME_OUT
              value: {{ .Values.worker.properties.config.bulker.bufferInactivityTimeOut | quote }}
            - name: CONSUMER_TYPE
              value: "system.event.properties"

          volumeMounts:
            {{- include "tracardi.volumeMounts" (dict "ctx" .) | nindent 12 }}

      volumes:
       {{- include "tracardi.volumes" (dict "ctx" .) | nindent 8 }}
{{ end }}